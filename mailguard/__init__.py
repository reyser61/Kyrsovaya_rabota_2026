"""MailGuard — система анализа почтовых логов (IMAP/SMTP)
для выявления фишинга и утечек данных.

Пакет состоит из:
  * parsers   — разбор логов Postfix (SMTP) и Dovecot (IMAP);
  * detectors — правила выявления фишинга, утечек (DLP) и компрометации;
  * engine    — оркестрация: парсинг -> детектирование -> инциденты;
  * storage   — сохранение инцидентов и статистики в SQLite;
  * reporting — генерация отчётов (HTML/CSV/JSON);
  * web       — веб-дашборд на Flask.
"""

__version__ = "1.0.0"
__author__ = "Курсовая работа"
